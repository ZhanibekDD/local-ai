"""Текст для /route: как устроен маршрутизатор."""

ROUTE_HELP_RU = """
Маршрутизатор (до основного ответа):

1) Текст: взвешенный скоринг по шаблонам (JSON / код / «документ без файла» / чат).
   Если разрыв между лучшим и вторым классом мал и включено ROUTER_LLM_DISAMBIGUATE —
   один короткий вызов Qwen-json уточняет класс.

2) Файл в Telegram → OCR-пайплайн + structured extraction.

3) Фото: подпись с «документными» словами → OCR; иначе → vision.

4) Подсказка внизу текстового ответа: модель и краткие reason-коды.

Подробнее: ROUTING.md
"""


def route_help_short() -> str:
    return ROUTE_HELP_RU.strip()
