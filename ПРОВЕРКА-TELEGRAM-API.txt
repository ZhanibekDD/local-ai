ВЫПОЛНИТЕ НА СЕРВЕРЕ:

REDACTED

curl -v https://api.telegram.org

curl -v --connect-timeout 10 https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getMe

sudo ufw status verbose

sudo iptables -L -n | grep -i drop
